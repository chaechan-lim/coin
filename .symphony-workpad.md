## Symphony Workpad

```
claude/coin-19:/tmp/symphony-workspaces/coin/COIN-19
```

### Plan
- [x] 1. Read project knowledge (CLAUDE.md, PROGRESS.md)
- [x] 2. Reproduce / understand current behavior
- [x] 3. Plan implementation approach for: BalanceGuard 선물 잔고 계산 방식 수정 + 내부 장부 재동기화 메커니즘
- [x] 4. Implement changes
  - [x] 4a. Fix `_fetch_exchange_balance()` to use correct futures calculation
  - [x] 4b. Add auto-resync mechanism to BalanceGuard
  - [x] 4c. Add `_auto_resync()` method
  - [x] 4d. Add config for auto-resync settings
- [x] 5. Write/update tests
  - [x] 5a. Test futures balance calculation (wallet - unPnL - margin)
  - [x] 5b. Test spot balance still uses free
  - [x] 5c. Test auto-resync triggers after N consecutive criticals
  - [x] 5d. Test resync resets paused state
  - [x] 5e. Test resync updates portfolio manager cash
  - [x] 5f. Test resync failure keeps paused state
  - [x] 5g. Test resync disabled (count=0) and no PM scenarios
- [x] 6. Run full test suite + lint
- [x] 7. Commit changes
- [x] 8. Update PROGRESS.md

### Acceptance Criteria
- [x] All relevant tests pass (1111 tests, up from 1091 baseline)
- [x] Code follows project conventions
- [x] No unintended side effects
- [x] `_fetch_exchange_balance()` uses walletBalance - unPnL - margin for futures
- [x] Spot balance still uses `free`
- [x] Auto-resync mechanism exists to recover from prolonged pause
- [x] V2 Tier1 engine can resume after resync

### Validation
- [x] Tests: 1111 passed
- [x] Lint: All checks passed (balance_guard.py + test_balance_guard.py)

### Notes
- Baseline: 1091 tests passing → Final: 1111 tests (20 new tests)
- Bug root cause: `_fetch_exchange_balance()` used `usdt.free` for futures
  - `free` = walletBalance - initialMargin (unrealizedPnL mixed in)
  - Correct: `total - unrealizedPnL - totalMargin` (matches PM init logic)
- Changes:
  - `balance_guard.py`: New `_calc_futures_cash()`, `_auto_resync()`, `is_futures` property, `set_portfolio_manager()`, `BalanceCheckResult.resynced` field
  - `config.py`: `balance_auto_resync_count: int = 5`
  - `futures_engine_v2.py`: Pass `auto_resync_count` and `portfolio_manager` to BalanceGuard
  - `tests/test_balance_guard.py`: 40 tests (was 17) — TestFuturesBalanceCalc (6), TestSpotBalanceCalc (2), TestAutoResync (8)
- Committed: `08cb8e1`

### Confusions
(none)
