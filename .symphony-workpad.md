## Symphony Workpad — COIN-15

### Plan
- [x] 1. Read project knowledge (CLAUDE.md, PROGRESS.md)
- [x] 2. Understand current BalanceGuard behavior
- [x] 3. Implement auto-recovery in BalanceGuard
- [x] 4. Add admin API endpoint for manual resume
- [x] 5. Enhance engine status with balance_guard detail
- [x] 6. Write/update tests (15 new BalanceGuard + 4 engine + 6 API = 25 new tests)
- [x] 7. Run full test suite
- [x] 8. Commit changes
- [x] 9. Update PROGRESS.md

### Acceptance Criteria
- [x] Auto-recovery: 3 consecutive stable checks (divergence < warn_pct) → auto resume
- [x] Manual resume API: POST /api/v1/engine/balance-guard/resume works
- [x] Status API: GET /api/v1/engine/balance-guard/status works
- [x] Event emitted on auto-resume
- [x] Existing tests still pass (20 original balance_guard tests kept)
- [x] New tests cover auto-recovery and API endpoint (25 new tests added)
- [x] No strategy parameter changes (no backtest needed)

### Validation
- [x] Tests: 1115 passed, 1 skipped (was ~1090, added 25 new tests)
- [x] Lint: ruff check on modified files passes clean

### Changes Made

**`backend/engine/balance_guard.py`**
- Added `auto_resume_stable_count` param (default: 3)
- Added `_consecutive_stable` counter
- Auto-recovery logic in `check_balance()`: when paused AND divergence < warn_pct → increment stable counter → after N consecutive stable checks → `resume(reason="auto_recovery")`
- `resume()` now accepts `reason` param for logging
- `get_status()` method for API introspection
- Fixed pre-existing f-string lint warning

**`backend/engine/futures_engine_v2.py`**
- `resume_balance_guard()` method for admin API
- `get_balance_guard_status()` method for status API
- `get_status()` now includes `balance_guard` detail dict

**`backend/api/dashboard.py`**
- `POST /engine/balance-guard/resume` — manual resume endpoint
- `GET /engine/balance-guard/status` — guard status endpoint
- Both use getattr() pattern for engine compatibility

**Tests (25 new)**
- `test_balance_guard.py`: TestAutoRecovery (8 tests), TestGetStatus (4 tests), TestBalanceGuardInit (3 new)
- `test_futures_engine_v2.py`: TestBalanceGuardAPI (3 tests), TestStatus (1 new)
- `test_api_engine_status.py`: 6 API endpoint tests

### Notes
- Previous session indicated completion but no code was found — implemented from scratch
- No strategy parameter changes → no backtest needed
- BalanceGuard is only used in FuturesEngineV2 (via SafeOrderPipeline)
- Config: warn_pct=3%, pause_pct=5%, check_interval=300s, auto_resume_stable_count=3

### Confusions
(none)
