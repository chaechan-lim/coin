## Symphony Workpad

```
claude/coin-13:/tmp/symphony-workspaces/coin/COIN-13
```

### Plan
- [x] 1. Read project knowledge (CLAUDE.md, PROGRESS.md)
- [x] 2. Understand existing architecture — read all relevant source files
- [ ] 3. Bug 1 (Critical): Add set_leverage() in SafeOrderPipeline.execute_order() before exchange execution
- [ ] 4. Bug 2 (Critical): Apply leverage multiplier to pnl_pct in Tier2Scanner._check_exits()
- [ ] 5. Bug 3 (Minor): Add _eval_error_counts, _position_trackers, pause_buying, resume_buying to FuturesEngineV2
- [ ] 6. Write/update tests for all 3 fixes
- [ ] 7. Run full test suite and validate
- [ ] 8. Commit changes

### Acceptance Criteria
- [ ] SafeOrderPipeline.execute_order() calls set_leverage() before exchange order
- [ ] Tier2 SL/TP calculation accounts for leverage (pnl_pct *= leverage)
- [ ] FuturesEngineV2 has _eval_error_counts={} and _position_trackers={} attrs
- [ ] FuturesEngineV2 has pause_buying()/resume_buying() stub methods for health_monitor
- [ ] All tests pass (789+)
- [ ] Code follows project conventions

### Validation
- [ ] Tests: `cd backend && .venv/bin/python -m pytest tests/ -x -q`
- [ ] Lint: `cd backend && .venv/bin/ruff check .`

### Notes
- 07:55 — Agent started on COIN-13
- Architecture analyzed:
  - SafeOrderPipeline (safe_order_pipeline.py) handles all v2 engine orders
  - Tier1 coins get leverage set in FuturesEngineV2.initialize() (line 147-153)
  - Tier2 scans 30 coins → SafeOrderPipeline.execute_order() never calls set_leverage → exchange default (20x) used
  - Tier2._check_exits() calculates raw price change % without leverage multiplier → SL/TP thresholds effectively divided by leverage
  - HealthMonitor._check_error_rate_trend() accesses engine._eval_error_counts → missing in FuturesEngineV2
  - HealthMonitor._check_position_consistency() accesses engine._position_trackers → also missing
  - HealthMonitor._check_api_health() calls engine.pause_buying()/resume_buying() → also missing

### Confusions
(none)
