## Symphony Workpad

```
claude/coin-15:/tmp/symphony-workspaces/coin/COIN-15
```

### Plan
- [x] 1. Read project knowledge (CLAUDE.md, PROGRESS.md)
- [x] 2. Understand existing architecture (BalanceGuard, FuturesEngineV2, API routes)
- [x] 3. Implement auto-recovery in `periodic_reconcile()` — 3 consecutive stable checks → auto resume
- [x] 4. Add config parameter `auto_resume_count` to FuturesV2Config
- [x] 5. Add admin API endpoint `POST /api/v1/engine/balance-guard/resume`
- [x] 6. Add admin API endpoint `POST /api/v1/engine/balance-guard/sync` (sync internal cash to exchange)
- [x] 7. Add `GET /api/v1/engine/balance-guard/status` endpoint
- [x] 8. Write/update tests for all changes
- [x] 9. Run full test suite and validate
- [x] 10. Update PROGRESS.md
- [x] 11. Commit changes

### Acceptance Criteria
- [x] Auto-recovery: 3 consecutive stable checks while paused → auto resume + event
- [x] Auto-recovery disabled when count=0
- [x] Manual resume API endpoint works
- [x] Balance guard status API endpoint works
- [x] Cash sync API endpoint works
- [x] All relevant tests pass (1089 passed)
- [x] Code follows project conventions (CLAUDE.md)
- [x] No unintended side effects

### Validation
- [x] Tests: `cd backend && .venv/bin/python -m pytest tests/ -x -q` → 1089 passed
- [x] Lint: No new lint errors in changed files

### Changes Made

**Files modified:**
1. `backend/engine/balance_guard.py` — Auto-recovery logic: `_consecutive_stable` counter, `auto_resume_count` param, `get_status()`, `consecutive_stable` property, `resume(reason=)` param
2. `backend/config.py` — `balance_auto_resume_count: int = 3` in FuturesV2Config
3. `backend/engine/futures_engine_v2.py` — Pass `auto_resume_count` to guard, add `balance_guard` property, `sync_balance_to_exchange()` method
4. `backend/api/dashboard.py` — 3 new endpoints: GET status, POST resume, POST sync
5. `backend/tests/test_balance_guard.py` — 18 new tests (TestAutoRecovery: 8, TestGetStatus: 3, TestInit: 2, TestCheckBalance: 2, existing updated: 3)
6. `backend/tests/test_api_balance_guard.py` — 8 new API endpoint tests
7. `PROGRESS.md` — COIN-15 entry added

**Commits:**
1. `feat: add BalanceGuard auto-recovery mechanism after pause`
2. `feat: add BalanceGuard admin API endpoints`
3. `docs: update PROGRESS.md with COIN-15 BalanceGuard auto-recovery`

### Notes
- 10:58 — Agent started on COIN-15
- Current behavior confirmed: `_paused = True` set on critical divergence or 3 consecutive warnings, but NO auto-recovery mechanism existed
- Root cause: `check_balance()` reset `_consecutive_warnings` on normal check but didn't check if `_paused` to initiate recovery
- Test count: 1089 passed (was 1021+ per PROGRESS.md, 789+ per CLAUDE.md) — no regression

### Confusions
(none)
