## Symphony Workpad

```
claude/coin-18:/tmp/symphony-workspaces/coin/COIN-18
```

### Plan
- [x] 1. Read project knowledge (CLAUDE.md, PROGRESS.md)
- [x] 2. Understand existing architecture — analyzed portfolio_manager.py sync_exchange_positions
- [x] 3. Reproduce / verify current behavior
  - Lines 1090-1091: position_cleared_not_on_exchange sets quantity=0 but does NOT return cash
  - Lines 1109-1127: futures cash handling block only logs audit, never modifies _cash_balance
  - Normal sell flow (update_position_on_sell L135/161): correctly does `self._cash_balance += margin + leveraged_pnl - fee`
- [ ] 4. Implement fix in portfolio_manager.py
  - In position clearing loop, after db_pos.quantity=0, add cash_returned for futures
  - cash_returned = max(0, invested + pnl_amount) — clamped for forced liquidation
  - Also update self._realized_pnl for consistency with normal sell flow
- [ ] 5. Write tests for COIN-18
  - Test futures TP cleared: cash increases by (invested + pnl_amount)
  - Test futures SL cleared: cash increases by (invested - loss)
  - Test forced liquidation: cash increases by max(0, ...) — may be 0
  - Test multiple positions cleared: cumulative cash return
  - Test spot position cleared: no change to this behavior (already working)
- [ ] 6. Run full test suite
- [ ] 7. Commit changes
- [ ] 8. Update PROGRESS.md

### Acceptance Criteria
- [x] Bug root cause identified: futures cash not returned on position_cleared_not_on_exchange
- [ ] Fix returns invested + pnl_amount (clamped to ≥0) to _cash_balance
- [ ] Fix updates _realized_pnl for consistency
- [ ] Existing 1021 tests still pass
- [ ] New tests cover: TP/SL/liquidation/multiple scenarios
- [ ] Code follows project conventions (structlog, no hardcoded exchange strings)

### Validation
- [ ] Tests: `cd backend && .venv/bin/python -m pytest tests/ -x -q`
- [ ] Lint: `cd backend && .venv/bin/ruff check .`

### Notes
- 15:01 — Agent started on COIN-18
- Bug: In sync_exchange_positions, when a futures position disappears from exchange, the DB position is cleared (quantity=0) but self._cash_balance is not updated. The margin that was subtracted at buy time is never returned.
- Root cause: Lines 1109-1127 have a futures policy "내부 장부 + Income API가 권위적 → cash 덮어쓰기 안 함" which prevents ANY cash update during sync for futures. This is correct for general cash balancing but incorrect for cleared positions — when a position's margin should be returned.
- Fix location: Inside the position clearing loop (around line 1091), not in the general cash handling block.
- Verified all 9 existing futures sync tests: none check cash_balance after sync, so adding cash return won't break them.

### Confusions
(none)
