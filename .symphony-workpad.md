## Symphony Workpad

```
claude/coin-19:/tmp/symphony-workspaces/coin/COIN-19
```

### Plan
- [x] 1. Read project knowledge (CLAUDE.md, PROGRESS.md)
- [x] 2. Reproduce / understand current behavior
- [ ] 3. Plan implementation approach for: BalanceGuard 선물 잔고 계산 방식 수정 + 내부 장부 재동기화 메커니즘
- [ ] 4. Implement changes
  - [ ] 4a. Fix `_fetch_exchange_balance()` to use correct futures calculation
  - [ ] 4b. Add auto-resync mechanism to BalanceGuard
  - [ ] 4c. Add `resync_from_exchange()` method
  - [ ] 4d. Add config for auto-resync settings
- [ ] 5. Write/update tests
  - [ ] 5a. Test futures balance calculation (wallet - unPnL - margin)
  - [ ] 5b. Test spot balance still uses free
  - [ ] 5c. Test auto-resync triggers after N consecutive criticals
  - [ ] 5d. Test resync resets paused state
  - [ ] 5e. Test resync updates portfolio manager cash
- [ ] 6. Run full test suite + lint
- [ ] 7. Commit changes
- [ ] 8. Update PROGRESS.md

### Acceptance Criteria
- [x] All relevant tests pass (baseline: 1091 tests)
- [ ] Code follows project conventions
- [ ] No unintended side effects
- [ ] `_fetch_exchange_balance()` uses walletBalance - unPnL - margin for futures
- [ ] Spot balance still uses `free`
- [ ] Auto-resync mechanism exists to recover from prolonged pause
- [ ] V2 Tier1 engine can resume after resync

### Validation
- [ ] Tests: `cd backend && .venv/bin/python -m pytest tests/ -x -q`
- [ ] Lint: `cd backend && .venv/bin/ruff check .`
- [ ] Format: `cd backend && .venv/bin/ruff format .`

### Notes
- 12:18 — Agent started on COIN-19
- Scope: `backend/`, `frontend/src/`
- Baseline: 1091 tests passing
- Bug: `_fetch_exchange_balance()` uses `usdt.free` (WRONG for futures)
  - `free` includes unrealized PnL and is walletBalance - initialMargin
  - Correct formula: `wallet = total - unrealized_pnl`, `cash = wallet - total_margin`
  - This matches `portfolio_manager.initialize_cash_from_exchange()` logic
- The internal cash_balance (PortfolioManager) = walletBalance - totalMargin - unrealizedPnL
- BalanceGuard compares `exchange_balance` vs `internal_balance (cash_balance)`
- For futures, BalanceGuard needs to compute exchange cash the SAME way as PM
- Need: `fetch_positions()` to get margin + unrealizedPnL for correct calc
- Auto-resync: After N consecutive critical checks, resync PM cash from exchange

### Confusions
(none)
