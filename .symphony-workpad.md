## Symphony Workpad

```
claude/coin-15:/tmp/symphony-workspaces/coin/COIN-15
```

### Plan
- [x] 1. Read project knowledge (CLAUDE.md, PROGRESS.md)
- [x] 2. Understand existing architecture (BalanceGuard, FuturesEngineV2, API routes)
- [ ] 3. Implement auto-recovery in `periodic_reconcile()` — 3 consecutive stable checks → auto resume
- [ ] 4. Add config parameter `auto_resume_stable_count` to FuturesV2Config
- [ ] 5. Add admin API endpoint `POST /api/v1/engine/balance-guard/resume`
- [ ] 6. Add admin API endpoint `POST /api/v1/engine/balance-guard/sync` (sync internal cash to exchange)
- [ ] 7. Add `GET /api/v1/engine/balance-guard/status` endpoint
- [ ] 8. Write/update tests for all changes
- [ ] 9. Run full test suite and validate
- [ ] 10. Update PROGRESS.md
- [ ] 11. Commit changes

### Implementation Details

#### 3. Auto-recovery in `periodic_reconcile()`
- Add `_consecutive_stable` counter (tracks checks where divergence < warn_pct)
- In `check_balance()`, when result is normal (not warning, not critical):
  - If currently paused: increment `_consecutive_stable`
  - If `_consecutive_stable >= auto_resume_count` (default 3): auto `resume()` + emit event
- Reset `_consecutive_stable` on any warning/critical
- Add `auto_resume_count` constructor param (default 3, 0 = disabled)

#### 4. Config parameter
- Add `balance_auto_resume_count: int = 3` to `FuturesV2Config`
- Pass to `BalanceGuard` constructor

#### 5. Admin resume endpoint
- `POST /api/v1/engine/balance-guard/resume?exchange=binance_futures`
- Access guard via `engine._guard`

#### 6. Cash sync endpoint
- `POST /api/v1/engine/balance-guard/sync?exchange=binance_futures`
- Fetches exchange balance and overwrites PM cash_balance

#### 7. Status endpoint
- `GET /api/v1/engine/balance-guard/status?exchange=binance_futures`
- Returns: paused, last_check, consecutive_warnings, consecutive_stable

### Files to modify
1. `backend/engine/balance_guard.py` — auto-recovery logic
2. `backend/config.py` — new config field
3. `backend/engine/futures_engine_v2.py` — pass config + expose guard
4. `backend/api/dashboard.py` — new API endpoints
5. `backend/tests/test_balance_guard.py` — new tests for auto-recovery
6. `backend/tests/test_dashboard_api.py` or similar — API tests

### Acceptance Criteria
- [x] Auto-recovery: 3 consecutive stable checks while paused → auto resume + event
- [x] Auto-recovery disabled when count=0
- [x] Manual resume API endpoint works
- [x] Balance guard status API endpoint works
- [ ] All relevant tests pass
- [ ] Code follows project conventions (CLAUDE.md)
- [ ] No unintended side effects

### Validation
- [ ] Tests: `cd backend && .venv/bin/python -m pytest tests/ -x -q`
- [ ] Lint: `cd backend && .venv/bin/ruff check .`

### Notes
- 10:58 — Agent started on COIN-15
- Current behavior confirmed: `_paused = True` set on critical divergence or 3 consecutive warnings, but NO auto-recovery mechanism exists. `resume()` is manual-only. `periodic_reconcile()` just delegates to `check_balance()` with no stability tracking.
- Root cause: `check_balance()` line 130-131 resets `_consecutive_warnings` on normal check but doesn't check if `_paused` to initiate recovery.

### Confusions
(none)
